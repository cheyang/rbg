== cluster ==
   server: v1.36.1-aliyun.1

== arm A: helm template | kubectl apply --dry-run=server ==
   ACCEPTED:
     clusterrole.rbac.authorization.k8s.io/rbgs-controller-role configured (server dry run)

== arm B: helm upgrade --dry-run=server (reusing existing release 'rbgs') ==
   ACCEPTED (whole release renders and validates)

== control: a sibling template from the same chart/cluster ==
   control ACCEPTED -> cluster and kubeconfig are usable

RESULT: F1 FIXED -- the ClusterRole is accepted by a live API server, and the
        whole release validates server-side.
