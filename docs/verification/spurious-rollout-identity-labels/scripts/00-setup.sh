#!/usr/bin/env bash
# Live-layer setup: prepare the real environment (install CRDs / create schema / etc.)
# Honor $KUBECONFIG (or your env's equivalent) so this runs unchanged on any machine.
set -euo pipefail
echo "TODO: setup for the live verification (scoped, idempotent)."
