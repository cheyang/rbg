=== R2-F14/F15/F16/F17: what does hack/gen-helm-rbac drop or mis-gate? ===
go version go1.24.1 linux/amd64

--- control-plain ---
  exit: 0
  emitted resources: pods get 
  emitted nonResourceURLs: 0
  gated blocks: 3

--- R2-F15 nonResourceURLs-only rule ---
  exit: 1
  stderr: gen-helm-rbac: config/rbac/role.yaml: 2 rules produced only 1 output blocks; a rule was dropped, likely one declaring no resources
  emitted: <no file written>

--- R2-F16 multi-document role.yaml ---
  exit: 0
  emitted resources: pods get 
  emitted nonResourceURLs: 0
  gated blocks: 3
  R2-F16 REPRODUCED: the second document was silently lost (daemonsets absent),
        exit 0. sigs.k8s.io/yaml.Unmarshal decodes only the first document.

--- R2-F14 unmapped resource beside a mapped one ---
  exit: 0
  emitted resources: apps replicasets get apps deployments get 
  emitted nonResourceURLs: 0
  gated blocks: 4
  R2-F14 REPRODUCED: 'replicasets' is emitted OUTSIDE the conditional (line
        21, gate starts at 24), i.e. granted even when the deprecated
        types are disabled. The gate list is a third hand-maintained copy of
        'which types are deprecated', and an entry missing from it FAILS OPEN
        (over-grant) with no test and no CI signal.

--- R2-F17 misspelled key + aggregationRule + wrong kind ---
  exit: 1
  stderr: gen-helm-rbac: config/rbac/role.yaml: parse: error unmarshaling JSON: while decoding JSON: json: unknown field "resource"
  emitted: <no file written>

--- determinism: 3 runs against the repo's real config/rbac/role.yaml ---
  OK: byte-identical across 3 runs (no Go map-iteration nondeterminism).

RESULT: generator robustness findings reproduced (see above).
        Note these are about the generator being the SOLE source of truth:
        F5's named failure mode (chart drifting from config/rbac) IS fixed and
        is CI-gated. What is new is that a drop or a mis-gate now fails silently.
