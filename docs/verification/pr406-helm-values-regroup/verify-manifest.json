{
  "topic": "pr406-helm-values-regroup",
  "pr": "https://github.com/sgl-project/rbg/pull/406",
  "harnessPaths": [
    "docs/verification/pr406-helm-values-regroup",
    "test/helm/values_regroup_verify_test.go"
  ],
  "layers": {
    "unit": {
      "kind": "gotest",
      "cmd": "go test ./test/helm/ -run 'TestStressDeployScript|TestRenderSucceedsWithNullGlobal' -json"
    }
  },
  "findings": [
    {
      "id": "F1",
      "polarity": "contract",
      "layer": "unit",
      "match": [
        "TestStressDeployScriptImageTagOverrideHonored",
        "TestStressDeployScriptTuningOverrideHonored",
        "TestStressDeployScriptReplicaOverrideHonored"
      ]
    },
    {
      "id": "F2",
      "polarity": "contract",
      "layer": "unit",
      "match": ["TestRenderSucceedsWithNullGlobal"]
    }
  ],
  "liveNote": "No live layer. All findings are provable with `helm template` (gotest unit layer). Requires the `helm` binary in PATH; tests self-skip if absent. F3 (pprof.port never a valid key) and F4 (no upgrade/NOTES.txt warning for renamed keys) are advisory notes in README.md, not gated by tests."
}
